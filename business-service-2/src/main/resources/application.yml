server:
  port: 8086

spring:
  application:
    name: business-service-2

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-change-this-in-production}

management:
  endpoints:
    web:
      exposure:
        include: health,info

server:
  port: 8085

spring:
  application:
    name: business-service-1

jwt:
  secret: ${JWT_SECRET:your-256-bit-secret-key-change-this-in-production}

management:
  endpoints:
    web:
      exposure:
        include: health,info
